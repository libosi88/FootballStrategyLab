> 本文件为按时间保留的开发记录，只认证当时的版本。当前源码为 0.7.0，状态见项目根目录 `本版实测与未完成项.md` 顶部与 `validation/_rationality_finish_20260916_r1/LATEST.json` 指向的报告；`validation/本版实际验收状态.json` 是 0.6.3 的旧文件，本文中引用的 `validation/v04/*`、`validation/.venv/setup_cache` 等路径已于 2026-09-16 清理。

# v3软件实现与验收进度

目标：将用户给定v3提示词的S0—S7及M00—M08实现为单联赛可复用软件，减少数百联赛重复模型调用。用户已授权连续实现、测试与交付。当前不是完整实现；不得以旧smoke或版本号代替覆盖验收。

需求原文：docs/v3_冻结需求原文.md；SHA-256 f6ea4ef9fd00ed84a2c5765e15f1d4997c9e680850c70fd104025e35f98b9772。

按用户最新要求，直接在原项目目录继续开发，复用既有解析、精确结算、界面、恢复、回放与交接代码。旧版交付ZIP及旧断点保持可审计。旧常规任务20260912_170227_941bbc已中断，原因未证明。

## 必须完成的工作

- [ ] A：冻结v3能力清单与语义版本，标准模式默认16方向；建立M00—M08真实分母与S0—S7阶段账。
- [ ] B：W/C完整数值、区间、时间、比分网格及所有历史基准、动态角色历史；不能按盈利选原子。
- [ ] C：T全部长度、跨度、逐步幅度、keep/reset、pulse/state、连续/可插入；复合盘水事件；独立小样本参考。
- [ ] D：前一完整分钟跨市场特征、明确陈旧度政策及赛前结束后的因果摘要；缺语义须BLOCKED。
- [ ] E：分片/外存掩码、可恢复枚举与精确零命中/缓存映射；规则数量=评价+证明/缓存+剩余；全部>10候选可恢复。
- [ ] F：全池状态、五种结算/风险/覆盖/利润集中度、减0.02与三主要压力、直接数值/路径扰动、可比较覆盖的替代与高风险/低样本名单。
- [ ] G：全池重叠与共同亏损、方向内/全局多起点加入替换移除、只赛前/只滚球/联合及本金政策比较、条件不确定性与选择偏差披露。
- [ ] H：持久化模拟执行名额/预占/幂等/未知/部分成交/恢复与第二信号影子；真实账户接口保持禁用。
- [ ] I：冻结名单独立验证入口、历史滚动重新发现/筛选及总联赛注册表，未运行不得写PASS。
- [ ] J：完整机器规则/特征/schema/正负临界样例/两包交接；标准完成状态须由实际覆盖和验收推导。
- [ ] K：单元/朴素参考/完整小样本标准空间/真实中超和阿尔巴超输入分别验收；运行资源不足时保留准确缺口，不能声称完整标准任务搜完。

## 边界

“所有规则”在本软件中对应附件明确定义的有限v3语法、网格与M00—M08；无限额外条件属于E扩展。实现全部生成语义与执行引擎，不等于已经把每个真实联赛的全部表达计算完。两者状态必须分开。

共享聊天页目前只渲染少量历史上传说明和最后两段回复；读到了两联赛验证、连续中断及本地agent接手要求，但不能声称208个历史提示的正文均已读到。最新用户说明和完整本地v3原文为当前需求依据。


## 当前实现记录（继续开发，不是最终验收）

主目录仍为FootballStrategyLab_v0.3；common.VERSION临时为0.4.0-dev。不要用旧v031发布清单/测试报告认证新代码。旁边的v0.4临时副本未使用；清理组合命令曾被自动审批拒绝，已只迁回代码，开发不受阻。

已经新增：
- standard_spec.py：完整W/C数值与区间/分钟网格、T所有长度/跨度/步幅/keep-reset/pulse-state/连续-可插入模板。
- standard_features.py、standard_kernels.py：因果新增特征、动态角色、完整绑定窗口、跨市场前一分钟、赛前摘要、独立Numba路径/窗口核。流式路径已改为增量有界状态，证据保存具体路径事件；已完比赛可释放特征状态。
- standard_columns.py、standard_atoms.py、standard_masks.py、standard_search.py、standard_mining.py：虚拟列、SQLite字典/别名、磁盘掩码类、完整事件安全收益上界、可恢复类搜索与原始表达展开。类等价只针对本批事件，不宣称未来等价。
- normalization.py、standard_review.py：同字段整数区间AND规范化、候选家族基础审查、逐归一规则单项/联合/语义扰动、SQL游标、高风险/低样本导出、惰性逐笔与可比较覆盖替代。
- pool_diagnostics.py：全部已登记盈利历史信号组的磁盘矩阵、两两重叠/共同亏损/相关、资料缺失与未交易分开、断点。
- paper_execution.py：SQLite事务预占、核心方向名额（固定主客归并）、完整批次幂等、未知/部分/取消/成交/结算回执、影子第二信号、报警。
- paper_runner.py：标准JSON事件到模拟执行协调、缺历史等待、回补消费历史首信号而不回填订单、断点与待提交批次。
- research_validation.py：已冻结名单共同比赛日块重采样、无损扩大水位单位、固定名单评测函数；滚动训练任务调度尚待接入。
- workflow_journal.py、engine_selftest.py：S0—S7阶段账与同代码/同测试版本的自检复用。pipeline开始接入先冻结全部方向再搜索，计数分开代表/等价映射/零命中/安全上界；最终标准完成状态尚待正确收口。

已运行的新增测试：test_standard_semantics（12）、test_standard_search（4）、test_standard_integration（1，全字典小样本约109秒）、test_standard_review（现5）、test_paper_execution（6）、test_paper_runner（最初4，后加回补回滚用例待总回归）。后续代码仍有修改，这些不是最终冻结版本的通过声明。

下一步重点：
1. 等待/查看validation/v04/development_tests.log当前快速全回归（排除较慢standard_integration）结果并修错。
2. 加入持久化模拟执行与历史订单的实际核对，补齐Runner/回执非法状态/缺历史/矩阵中断的测试。
3. 实现按SID分割、重新读取训练子集以独立冻结水位精度与特征域的walk-forward任务；测试先冻结名单再评测，不能重放全样本赢家冒充滚动。
4. 完成冻结名单评测/标准JSON模拟/原始候选展开CLI与A包自包含入口；生成特征字典、JSON schema、正负临界样例和中文卡完整说明。
5. S0—S7及M00—M08报告、联赛总注册表与最终STANDARD_HANDOFF_COMPLETE门禁。当前pipeline仍保留旧PARTIAL_CORE文案和有限完成状态，不能提前改成成功。
6. 更新网页standard选项与参数、精确大整数计数、标准覆盖/筛选/风险/触发/未来验证不同状态。
7. 最终全回归、完整小样本标准流程、真实中超与阿尔巴超分别验证；实际大任务没跑完必须保留准确剩余，不能称标准已完成。

注意：当前默认standard、16方向、跨市场5分钟沿用原研究报价年龄假设并冻结basis；不等于成交认证。历史流要按同场时间排序。STATE_SCHEMA已变为FSL_signal_state_v4；standard信号必须先明确历史完整。verify已经给完整历史回放标记ready，未来实时使用应通过Runner的回补/开场声明接口。


## 2026-09-12 21时：当前联调进展

原项目新增paper_verification、handoff_assets、workflow_gates、packaging、global_bound，pipeline已串联真正S0—S7门禁。标准完成需要全16方向scopecomplete、剩余0、全池复核、组合搜索、引擎自检、逐条样例、持久化模拟四情景和实际ZIP空目录验收全部通过。旧打包函数已删除。完整包与部分观察包不同名称。界面standard默认、精确大整数、阶段账和失败门禁已做实际浏览器检查，无JS错误。

已修复W高精度水位区间起点按0.01、所有指定基准涨跌模板零命中也登记、SQLite绑定拒绝时关闭连接、风险矩阵形状/缺失校验。新增全事件逐场最大收益上界快速路径，在仍生成完整字典和可逆表达索引的前提下覆盖必然不过10的任务。

冻结代码完整算法测试141项通过，日志validation/v04/frozen_engine_tests.log。随后只修复被嵌套自检排除的全链路测试CSV字段“大小”为“大小球”，未改lab引擎。第一次合成全链路任务已暂停，不能标通过；新任务validation/v04/full_standard_acceptance_v2/workspace/20260912_210748_e77013正在实际跑16分支，完整验收日志full_standard_acceptance_v2.log。

真实数据语义回归程序validation/v04/run_real_semantics.py正在运行，先阿尔巴超再中超；涵盖各16方向的基准、窗口、角色、跨市场、赛前摘要、插入路径和复合路径固定探针。它验证真实数据上的批量与流式/持久化执行一致性，不是全标准盈利规则搜索或推荐名单。日志validation/v04/real_semantics.log。

当前开发测试进程：full_standard_v2 exec session48176；real_semantics exec session29728。原正式UI仍旧进程port8765(PID41584实际解释器/35348启动器)，旧routine任务INTERRUPTED，旧首次任务PAUSED。新版UI测试服务port8876，用独立validation/v04/ui_workspace，启动器PID14820，含明确合成的大整数显示测试任务；不要当生产研究。

下一步：等待并修复完整链路与两真实联赛语义回归；对照更多缺口，按实际计算范围决定真实标准搜索规模；最终版本/源清单/ZIP和当前验收报告尚未最终发布。旧v031状态已移存validation/v031/历史发布验收状态.json，根validation/本版实际验收状态.json现在明确开发验收中。不要提前宣称项目所有需求已完成。


## 最终候选代码冻结（0.4.0）

当前lab源码指纹0c30afa5cbe7faa2f785f5ce1053b779d94d3ef67fba9ec28a268fdd3e132d37，完整引擎147项已通过，日志在validation/v04/final_standard_acceptance/workspace/20260912_214319_a3970f/engine_tests.log，engine_validation记录源码与测试未变。

已正式接入standard_feature_cache.py：内存映射临时矩阵，一次因果扫描保存16方向npz缓存，逐列与旧单方向实现一致，暂停只复用完整提交缓存，磁盘不足保留范围。已修：dictionary_complete的SQLite内容SHA，mask成员映射checksum，研究末/打包时源码身份，registry实际日期/审查层计数，实时match_end丢弃未提交批次与缺历史队列、拒绝重开，Runner构造失败关闭连接。

之前开发版全链路已实际通过：full_standard_acceptance_v2/workspace/20260912_210748_e77013；684895779684表达发生数，16个基线实际评价，其余由全事件收益上界证明不过10，剩余0；A包空目录141测试和触发核对，B包生成。仅是明确合成的小样本完整语法验收，不是真实联赛盈利挖掘。

旧真实阿尔巴超回归：57393事件、100条有命中的固定语义验收规则、10786信号，所有信号/四情景订单0差异；原始输入哈希一致。旧中超重复扫描测试已停止改为一次扫描，未声称通过。原型在阿尔巴超108组探针10786信号一致，整份特征31.4秒；小样本317事件逐列相同，0.17秒对2.28秒，不能把倍率外推全流程。

当前实际运行：
- exec session63225：最终完整标准流程，job 20260912_214319_a3970f，目录validation/v04/final_standard_acceptance/workspace；日志final_standard_acceptance.log。
- exec session20444：最终两真实联赛语义回归，输出validation/v04/final_real_semantics，日志final_real_semantics.log；先阿尔巴超后中超。

不要在这些运行中修改lab或tests，免得混版本。当前有个内部进度字段direction会沿用特征缓存末方向LIVE_AWAY，S1实际冻结方向须看mining/*/dictionary_complete.json；网页S1不显示这个旧方向字段，不应按它汇报接近完成。

剩余收口：等最终全链路/真实回归结果，处理实际失败；更新README、根本版实测与未完成项、validation/本版实际验收状态.json为真实最终状态（PASS_WITH_DECLARED_SCOPE仅在通过后）；运行已准备的validation/v04/build_source_release.py生成并空目录验证源码ZIP和manifest；更新原服务8765（目前仍旧进程41584实际/35348启动器），关闭独立UI测试服务8876（31004实际/14820启动器）并验证最终UI。source_release脚本会拒绝提前打包。普通真实联赛全标准挖掘尚未完成，不得拿固定语义探针/合成上界证明冒充。源码zip预定桌面FootballStrategyLab_v0.4.0_标准研究版.zip，须实际生成与验证后再链接。


## 用户P1—P5审查后的当前状态（2026-09-12 23:36）

当前源码冻结为b8ae749b067a2f061aac7d347b8a1bfc45e9160ec8e18971e3f3069570eb7bdf；168项测试通过（user_review_fixes_tests.log）。不要在实际成本测量期间改lab/tests。

已经修复并验证：空名单不能授予STANDARD_HANDOFF_COMPLETE；A/B先在私有目录完成、整bundle原子发布才写完成；缺日期/封盘列拒绝，catalog缓存v2；LIVE AH缺比分、适用报价无合格赛果等空数组明确S0阻塞；全封盘/不适用盘口保留合法零机会分类。所有lab os.replace统一atomic_replace，Windows重试15秒；实际1.5秒句柄锁二进制及JSON替换通过。PaperDispatcher已WAL+FULL及sid索引，独立落盘200单对照3.78倍、语义和重试一致。

P1第一次非空真实生产链路：workspace/v04_nonempty_standard_acceptance/20260912_225858_ad27a8；合成90场、单LIVE_OVER、10节点M01，3599候选全部复核、3588规范化规则、7202邻域、合格规则3149、盈利历史组4/合格2、主1备1，每份90信号、四情景各90单且0差异；ADD/REMOVE/REPLACE比较12/6/6。但生产B包校验误将tuple正常JSON转list当损坏，失败未发布，故不能标交付完成。已准备唯一一行修复及2项测试，放staged_fixes/packaging_tuple_fix.py、test_packaging_publication_tuple.py；必须等当前成本跑完再应用，以免源码身份改变。应用后重新跑非空验收，不需要再用空数据“通过”证明完整。

P2真实中超成本：原C工作区workspace/v04_real_budget_cost/20260912_225900_362357，677场/667合格、359638报价/LIVE_OVER171730可评；65536节点预算。首次准备24分07秒，字典1,223,481原子尚未完成，无masks/search，不能报节点率。因C盘磁盘风险正常Store暂停，报告attempt001保留。

C盘骤降来自SearchIndexer约62MB/s写入；Stop-Service WSearch权限不足失败，未改系统服务。占用随后自动释放。只对项目workspace/validation/.venv/setup_cache加NotContentIndexed，共9779项；原始CSV未动。为隔离干扰，已逐文件SHA核对复制活动成本工作区到D:/FootballStrategyLab_workspace/v04_real_budget_cost（原C副本保留不再运行）。恢复脚本新增--resume-job严格校验、复用gzip不改hash、保留旧attempt耗时。当前exec session33221正在D盘恢复同一JID/b8/65536，日志real_budget_cost_b8_resume.log，重复字典前缀成本单列。等待它真正进入mask/DFS并完成节点样本。

P4文档：按国家分类数据和指数下载工具/修复后数据的挖掘注意两份口径文件均标“历史口径，已不适用当前单联赛软件”；两份说明索引也标历史，原文及SHA在data_doc_backups。

P5：旧FootballStrategyLab_v0.4已改名FootballStrategyLab_历史快照_旧v0.4目录_20260912，代码字节保留并加历史说明。当前源码仍原v0.3目录，需所有计算停稳后改名FootballStrategyLab_v0.4.0；考虑保留原v0.3路径为指向新主目录的junction兼容现有Codex/历史记录。原UI服务已为更新停止，最后服务器PID42920/32104已退出，8876测试服务也停止。完成后需启动新主目录8765并实际验证。

历史两份空标准“完成”证书已纠偏：full_standard_acceptance_v2及final_standard_acceptance的可变workflow/summary/acceptance/Store改PARTIAL_RESULT+empty_roster_chain_verified，原JSON在historical_metadata，旧A/B ZIP字节和SHA未改，旁有历史归档失效声明。旧build_source_release.py依赖旧空验收，必须改成新非空验收与真实节点成本报告，不能重用原门禁。

下一步：D盘成本完成后冻结逐文件证据；应用tuple打包修复及2测试、全回归、重跑P1非空任务。成本采样代码若只与最终代码在这一行发布校验上不同，要保留原b8测量身份，并用frozen_engine_b8/files.json及旧packaging.py证明所有搜索代码完全一致，不虚改测量hash。再做源码ZIP实际空目录测试、正式验收报告、README、主目录更名与启动服务；明确有限预算不等于真实全联赛挖掘完成，473计划go/no-go据测量实际阶段判断。

## 2026-09-13 最终修复验收收口

29版源码仅对b8增加打包元数据逐字节比较修复，完整170项测试通过。P1新任务20260913_013535_2e27df实际完成23项合成非空断言，A解压170项测试与回放、B CRC/内容、再次解压A回放均通过；生产状态为PARTIAL_RESULT，未宣称完整标准空间或真实名单。

P2主预算65536节点与M02/M03各4096节点均完成；小账本、SHA和同源计划绑定已独立核对。快照validation/v04/delivery_evidence/p2_b8_20260912_172940保留39份原文件与6份派生证据。473联赛全量生产仍不放行；M04—M07及真实全池筛选没有完整规模实测。

版本入口FootballStrategyLab_v0.4.0为目录junction，指向原项目同一套源码。Windows当前会话共享占用阻止物理改名，故原v0.3目录隐藏保留，未删除或复制源码。旧v0.4已改为历史快照。入口Python和实际浏览器检查通过，当前UI证据绑定29源码及完整web资产。

最终源码封包以build_source_release.py门禁与实际空目录测试为准。源码验收状态先冻结，ZIP成功后只另写外置验收证书，不回写manifest内文件，避免内容清单自失效。
