<!-- FSL_070_CURRENT_ENTRY -->
> 当前源码为0.7.0；下方0.6.3与旧日期的测试数字仅为历史记录，不能认证当前源码。
> 当前功能见 docs/0.7.0_使用与验收入口.md；实际状态以 validation/_rationality_finish_20260916_r1/LATEST.json 指向的报告为准。

# 当前实现与验收进度

当前源码版本 0.7.0（以 `lab/common.py` 的 VERSION 为准）；0.6.3 的可调历史盈利池门槛与四项可靠性修复已包含在内，0.7.0 另加入中等搜索规格 balanced_v1 与历史证据政策（研究段固定日历年、小段亏损阻断、小段样本不足只记证据不足、缺完场状态默认隔离）。当前有效范围以随包 docs/当前交付范围.md 和根目录本版实测与未完成项.md 为准。

旧版开发勾选记录已移到 [历史记录](history/v3_实现与验收进度_历史截至0.4.3.md)，不能作为当前完成证明。

0.6.3 软件级验收已完成：441 项默认单元测试通过（2 项按设计跳过），冻结引擎回归 439 项通过，空/非空 16 方向合成整链、依赖核对和真实 Chromium 界面均 PASS。非空合成用例故意把每方向搜索限制为 10 个节点，所以 S2/S3 正确保持 PARTIAL；它验证的是部分搜索状态和后续交接链路，不是实际联赛完整搜索证书。

538/536 与上段 441/439 分别只认证 2026-09-15 修订与 0.6.3 的冻结指纹，都不认证 0.7.0。当前测试项数、通过与跳过以本轮实际结果为准，见 `validation/_rationality_finish_20260916_r1/LATEST.json` 指向的报告；状态不是 PASS_SCOPED_070 或指纹不一致时即未通过。未发布修订说明见根目录 `本版实测与未完成项.md` 顶部。

尚未完成：真实联赛完整标准搜索与交接（至今没有跑通过一次；旧的印度超试跑断点与其证据已于 2026-09-16 清理）、真实联赛滚动验证。真实资金/第二笔执行未开放。

0.6.0 起默认目标为全部选定历史中的稳定高净胜研究；0.5.0 留出、净胜>10 与只在即时盘触发保留为显式 `research_objective=validation`。0.5.0 由用户确认按合理性审查实施、并仍适用于验证模式的做法：
- 默认搜索语法改为预先登记的紧凑语法（最多两个条件、粗网格、可以算完）；v3 完整语法保留为 search_grammar=v3_full，但真实联赛算不完，只能部分覆盖并据实标为观察。
- 第二阶段门槛由绝对值（80场、每年15场、连亏≤4、回撤≤5、组合回撤10/6）改为与样本量相关的比例口径：场次按方向可用比赛数折算，年份改为研究段，风险用收益回撤比，连亏只作报表列。
- 验证模式留出最近12个月作样本外，名单冻结后评测一次，并披露检验次数与校正后显著性；只有样本外确认为正才推荐进入模拟。历史模式不强制留出。
- 验证模式赛前规则只在即时盘触发；历史模式默认早盘与即时盘都触发。跨市场陈旧度按阶段区分（滚球5分钟、赛前24小时）。
- 邻域扰动步长改为有实际差别的量（水位±0.05、盘口±0.25、分钟±5）。
- 组合容差带内先比较最大回撤，再比较证据量与条件数；容差改为最优净胜的相对值。
- A 交接包保持可直接运行的 `results/`、`lab/` 布局，以 `results/最终决定.md` 与 `缺口说明.md` 给出结论和缺口；高风险与低样本观察全表放在 B 审计包。
- 空目录验收复用已安装依赖，但核对其版本与 requirements.lock 一致；不联网重装。

仍待用户决定：复合盘水路径的时限版本（复合T）是否登记进 v3 完整语法（紧凑语法本身不含路径时限参数）。
