<!-- FSL_070_CURRENT_ENTRY -->
> 当前源码为0.7.0；下方0.6.3与旧日期的测试数字仅为历史记录，不能认证当前源码。
> 当前功能见 docs/0.7.0_使用与验收入口.md；实际状态以 validation/_rationality_finish_20260916_r1/LATEST.json 指向的报告为准。

# 2026-09-16 全面审查后的逐条修复轮（未发布）

按 8 个领域的全面审查结论，逐条修复其中核实属实且可安全修复的问题。未改变历史净胜口径、研究门槛、搜索语法、赛前触发窗口与真实执行权限；真实下单与第二笔仍关闭。本节不预填验收结论。

输入与目录：质量交割清单除按数据树向上查找外，也在所选目录或解压后的 ZIP 内向下查找（跳过“_”开头的中间目录），同一根目录内发现多份时报错；支持市场与状态的报价行缺 sId 或变化时间非法直接报错，不支持的市场/状态行按 `unsupported_market_or_status_rows` 计数跳过；索引行非空却无法解析的终场比分按畸形标签隔离该场并计数，不因另一行补齐而消失；ZIP 内重复或仅大小写不同的同名成员拒绝解压；滚球与赛前跨市场陈旧度必须同时填写或同时留空（此前只拦截其中一个方向，另一方向会被下游静默丢弃）；`read_json` 对 Windows 并发读写的 PermissionError 重试。

研究与筛选：主备重叠诊断与全池风险矩阵的“共同可观察比赛”改为按方向计算，并按研究口径取成交（历史模式为剔除进球前拒单的情景 4），矩阵绑定升为 V4 并记录 `trade_basis`；替代对照池排除与在册规则逐笔签名相同的孪生候选（无签名的候选不再被误判为孪生）；覆盖跨度按日历天计算；全池两两对照达到预算以 `BUDGET_STOP` 封存，且纯预算键不再进入范围绑定——调大预算可以在已提交矩阵上继续算，不再报“范围改变”。

触发、模拟与交接：`match_start` 在该场已有等待历史的报价时拒绝，须改用 history 回补；重发已完整比赛的历史不再丢弃该场合法的待提交批次；回补后按该场最新已观察分钟一次性提交，超过提交时限的首信号按规则拒绝并报警；早于本场已处理时间的报价按迟到消息拒绝，协调器不再因此故障停流；交接结果缺少 `boundary_cases.json` 时验收失败，不再在验收时重新生成后自我比对；名单导出新增第二阶段批量研究首触发参考 `research_first_signals.jsonl.gz`，触发核对将在线逐条信号与该参考逐条比对，缺漏或多出即 FAIL；经济门禁与文档不再把共用同一派单代码的复算称为“独立重算”；触发卡压力情景保留率分母按拒单前订单数计算。

运行与工具：启动脚本正确识别“工作区已有其他版本工作台”的退出码 3；联赛登记表更新移出正常返回路径，失败只写 `league_registry_error.log`；多机任务复用时按 sha256 核对冻结输入是否仍在首次导入的位置；打包子进程在 Windows 上按进程树终止；重建扫描跳过无法读取的任务目录；命令行 `--no-budget` 帮助文案与界面默认值一致；搜索停止证据遇热回滚日志时先回滚再只读核对。

测试与文档：新增 `tests/test_full_fix_paper_stream.py`、`tests/test_full_fix_data.py`、`tests/test_full_fix_integrity.py`、`tests/test_full_fix_selection.py`；`docs/机器规则与数据契约.md`、`docs/标准流程与模拟接口.md`、`docs/当前交付范围.md`、`README_先读.md` 按当前 0.7.0 代码更新。另修正 `tests/test_pipeline_standard.py` 中遗留的进球前拒单政策常量期望：0.7.0 已将该常量改名为 `explicit_live_close_and_score_change_within_one_minute_v2`，而该端到端用例默认跳过、只在非空验证链路运行，因此默认单元测试查不出这处不同步。另修正本轮新增的界面鉴权用例：A 包空目录验收会在解压副本内跑全量测试，而整链的流水线车道最多两条并发，服务端在读取请求体之前返回 403 时，Windows 会把它表现为连接重置而不是 403 响应；用例现同时接受 403 与连接重置，并另行断言服务仍能正常应答，避免把服务崩溃误判为拒绝。

验收：本轮全部编辑结束后按 `docs/0.7.0_使用与验收入口.md` 的入口重跑整链，结果以 `validation/_rationality_finish_20260916_r1/LATEST.json` 指向的报告为准。需要说明的是，上一次 0.7.0 整链在 `historical_full` 阶段失败的原因是验收进行期间源码仍在被编辑（`打包期间发布源码集合已改变，拒绝发布混合版本`），属于发布保护按设计触发，不是计算缺陷；本轮在冻结后重跑。

# 2026-09-16 七项问题修复与相邻批次一致性修订

本轮修改模拟账本的不可回退时间、撤余后已有成交结算、A/B 同源快照与发布白名单、调度异常存活及健康提示、旧审计包模式兼容、旧运行任务暂停入口。另修复同一决策时刻积压的不同首信号批次发生冲突的问题，合并后仍按冻结优先级、相反方向规则和原有整场上限处理。未改变历史净胜口径、研究门槛、搜索语法或真实执行权限。

修复基线已保存在 `validation/_repair_seven_20260916_r1/baseline_source.zip` 和 `baseline_identity.json`。变更与复验入口见 `docs/修复说明_20260916.md`。最终状态只认本轮 `repair_result.json` 及对应源码指纹；测试数、失败与跳过项、端到端及界面验收范围均由实际结果记录，不在验收前预填通过。旧任务不得直接用新引擎续算；已运行的旧界面不会因文件被编辑而自动加载修复。

以下为历史记录，不认证本轮源码。

# 2026-09-15 审查问题核对修订（未发布）

逐条核对 `审查发现_待修复问题清单.md`，只修复核实属实的问题；该清单已按使用要求删除，核对结论记在本节。

本轮修改：
- 清理 `lab/` 中未使用的导入及 `common.py` 一处未使用的赋值（`features.py` 的 `LABEL_FIELDS` 由测试从该模块导入，保留）。计算逻辑不变，但引擎指纹随之改变；此前绑定原引擎指纹的全面审查、迁移修订和交付审计证据不再认证当前源码。主工作区现有任务均绑定更早版本的引擎，不受影响。
- 删除与 `docs/v3_冻结需求原文.md` 字节完全相同的 `docs/原始研究标准v3_本版尚未完整实现.md`（历史记录按前者的文件名与哈希引用）。
- 更正下方 0.6.2 小节的冻结记录：0.6.2 冻结目录与 `frozen-v0.6.2` 标签实际不存在，0.6.2 起的修订均未提交。
- README 注明命令行须使用项目 `.venv` 的 Python，并补充两条端到端流水线的运行方式；`docs/当前交付范围.md`、`docs/v3_实现与验收进度.md` 补充当前测试数量。
- `tests/test_v031.py` 同时捕获命令行测试的标准错误输出，不再把 MagicMock 打印到测试日志。
- `validation/delivery_audit_20260915_r1/clean_install.json` 的 FAIL 系检查脚本正则缺少反斜杠所致（日志实际为 538 项测试通过、2 项跳过）；修正后的脚本在本轮证据目录重新运行：首次重跑因 DNS 不可用仍为 FAIL（`_findings_fix_20260915_review/clean_install.json`，原因 `DNS_UNAVAILABLE_PYPI_NOT_RESOLVED`），DNS 恢复后的重试为 PASS（`_findings_fix_20260916_install_retry/clean_install.json`）；该 PASS 只认证当时的引擎指纹 `03d90eb8…`，0.7.0 的干净安装检查尚未运行。

核对后未修改：同场同市场跨分钟反向成交符合 v3 冻结政策（冻结需求第 186 行，名单包 `opposite=reject_current_minute_market`），改动须另立政策版本；槽位口径、memmap 物化、比赛状态残留、SBOM 含 pip（来自 `installer.lock`）、审查后发布指纹漂移（迁移修订已重新绑定）等疑点经核实不成立；清单文件排除计算与表头重复检测的复杂度在实际规模下可以忽略。

本轮证据以 `validation/_findings_fix_20260915_review/` 为准。

# 2026-09-15 旧任务迁移修订（未发布）

单条及批量重建支持显式标准参数迁移、JSON配置覆盖和只读预览；批量可按任务ID筛选，失败返回非零退出码，成功、失败和跳过分别报告。迁移记录列明参数差异，旧模式和预算默认保留。config/rebuild_historical.json提供显式全历史模板，不改旧任务断点。只读预览使用私有注册表及WAL一致快照和临时扫描缓存，不让SQLite在原工作区新建附属文件。旧任务界面区分当前状态与上次阶段记录。

本轮证据以 validation/_migration_20260915_review/ 下的实际测试和报告为准。迁移、命令行、配置模板和网页属于发布文件集合变动；计算引擎未修改，原516项及旧整链证据只认证各自发布文件集合，不能替代本轮验收。

# 2026-09-15 全面审查修订（未发布）

本轮继续审查当前可执行源代码、相关调用与测试。新增修复包括重复标签缺失值合并、重复CSV表头拒绝、配置类型、多机任务完整输入身份、预处理与筛选缓存证据、停止搜索与组合日志校验、字典只读和封存、分钟条件归一化、触发对象独立副本、交接Schema及配置核对、打包失败保留旧结论、A/B字节一致性、界面设置与分页、停止调度和启动资源清理。

已修问题清单、逐文件审查覆盖、未执行范围及实际验收结果位于 validation/_audit_20260915_full_review/。只认绑定当前引擎与文件集合的证据，不把旧测试、单项通过或有限搜索写成完整发布。真实原始数据、既有任务、旧冻结版本和账户配置保留，真实下单及第二笔仍关闭。旧断点只能用对应冻结源码继续，或新建／重建任务。

以下为此前修订的历史记录，保持原样，仅认证其当时的源码和测试。

# 2026-09-15 首轮工作区审查修订（未发布）

原工作区指纹 `d3463b18c5ba9dcea405e27bdc71f4cb022957093f1fc7447648a678a5af0102` 的隔离回归已重新运行，439 项通过，源码与冻结副本均未改变，证据位于 `validation/_audit_20260915_review/baseline/`。

本轮复现并修复了缺失滚球比分导致的大小球质量过滤遗漏：前一报价比分为 2–0，下一报价比分留空、大小球盘为 1.5 时，旧代码仍生成买大记录，按 0.94 水位计入 +0.94 净胜。现在沿用同场、同市场、同阶段此前最新的已知比分判断盘口是否不合理；原始比分字段保持缺失，明确的比分纠正可以替换此前已知比分，不读取终场比分来补当前比分。

新增 `tests/test_missing_live_score_quality.py` 的 6 项测试：修复前 3 项失败、3 项通过，修复后 6 项全部通过，覆盖异常报价、交易与净胜、触发信号、正常缺失比分、比分纠正和跨场隔离。SBOM 已按当前引擎重新生成。

修复后完整回归、默认历史模式空／非空交接链路的结果以 `validation/_audit_20260915_review/final/` 中的独立证据为准。该目录的审查结论单独披露未执行的发布验收；本轮未新增 Git 发布标签。修改引擎后的任务应使用新的冻结配置，不能混用原指纹断点。

以下为原 0.6.3 及更早版本的证据，仅认证各自绑定的指纹。

# v0.6.3 盈利池最低净胜可调

按使用要求：两年以上数据再收净胜刚过 0 的规则没有意义。历史模式第一阶段改为按任务冻结的最低净胜收集，默认 10 单位；界面可改，0 仍表示净胜>0 全收。留出期仍然禁止。已开跑的 0.6.1 印度超任务继续用当时冻结的 >0，不能热改。

本轮同时完成一次针对交付链路的可靠性修复：
- 滚球比分已经变化但盘口与两边水位仍停在上一笔时，该报价会持续隔离到盘口或水位真正刷新；滚球大小盘低于已知总进球的报价也会直接标记为 `quality_blocked`，避免进球前陈旧报价制造历史虚假盈利。
- 索引 CSV 的空白完场比分、日期、开赛时间或结果状态，不再覆盖报价文件中已经完整的同字段，避免完整比赛被误删。
- 历史模式界面预览改为读取历史决策实际使用的情景 4（分钟末取价并剔除进球前拒单），验证模式仍读取情景 0；独立浏览器验收用同一组合构造出情景 0 净胜 12、情景 4 净胜 7，页面实际显示 7。
- 标准搜索候选读取按任务实际 `search_backend` 验证；已由 `standard_global_bound` 等非 DFS 后端证明完成时，不再错误打开 DFS 账本。

本轮最终验证证据保存于 `validation/revision_063/`：
- 默认单元测试：441 项通过，2 项按设计跳过，0 失败。
- 冻结引擎回归：439 项通过，源码与冻结副本均保持不变；引擎指纹为 `d3463b18c5ba9dcea405e27bdc71f4cb022957093f1fc7447648a678a5af0102`。
- 16 方向空结果整链：PASS。完整跑完并正确接受空名单，不制造策略；真实 A 包解压后自检与回放通过。
- 16 方向非空有限链：PASS。S4/S5/S6 与 S7 交接包均通过，真实 A 包解压后自检与回放通过；该合成用例每方向只给 10 个搜索节点，因此 S2/S3 按设计标为 PARTIAL，用于验证“未搜完就必须诚实标部分结果”，不能当成真实联赛完整搜索证书。
- 依赖核对：PASS。安装版本与锁文件、官方哈希一致，检查时未发现活动中的已知漏洞记录。
- 真实 Chromium 界面验收：PASS，版本、连接、旧设置迁移、保存、历史/验证模式切换、结果文件过滤和历史情景口径均通过，无脚本错误。

边界与后续：
- 本轮没有启动真实下注，真实资金与第二笔仍关闭。
- 本轮软件级合成验收已经覆盖空/非空 16 方向整条流水线，但真实联赛的完整标准搜索与交接仍需由冻结版本单独跑完后才能认证该联赛结果。
- 最终冻结只认 `validation/revision_063/` 与其生成的 `validation/frozen_releases/0.6.3_*` 证据；0.6.2 及更早记录仅认证其对应历史指纹。

以下为 0.6.2 及更早版本的历史证据：

# v0.6.2 组合比较提速

本轮只加速历史模式组合比较，不改变盈利池、筛选门槛、取价口径和搜索结果。邻域只排序一次；访问记录按规则编号比较；替换时若该场在一方首笔报价前已达整场上限，直接沿用已算结果；暂停标记每半秒读取一次。随机对照与分段暂停续跑与完整跑逐字节核对（`tests/test_search_speed_062.py` 4 项）。

本轮验证（最终引擎 7ba63934…，证据位于 `validation/revision_062/`）：
- 默认单元测试（最终源码副本）：436 项通过（2 项端到端默认跳过，另行运行），161 秒。
- 历史模式空结果链路：PASS，459 秒。3 场全部参与，盈利表达发生数 373，剩余 0，状态 HISTORICAL_RESEARCH_COMPLETE。
- 历史模式非空链路：PASS，597 秒。90 场全部参与，盈利表达发生数 47，主备名单各 4 条；组合比较使用按场增量重放并达到局部最优。主名单共同回放 360 笔，净胜 85.5 单位，按场回撤 8.0 单位。每方向 10 节点预算使剩余 1,315，正确标为部分结果。
- 验证模式空结果链路：PASS，460 秒。验证模式非空链路：PASS，557 秒。结果与 0.6.1 相同：主名单 2 条（滚球让球方净胜 15.6，滚球大球净胜 22.8），样本外确认，A 包解压后回放通过。验证模式仍按五个情景逐单回放比较组合。
- 浏览器界面（Cursor 内置浏览器，独立端口 18772，工作区复制自历史非空验收任务并写入 0.6.0 风格的旧组合上限；未触碰 8765 服务）：27 项通过，无脚本错误。旧上限未带入新任务；最终决定预览含共同回放合计。
- 依赖核对：PASS。隔离冻结回归：写完本文件后对最终源码运行。

边界与后续：
- 本轮未修改复盘系统的现行策略和实盘配置，未运行真实下注。
- 印度超 0.6.1 冻结副本试跑（`validation/trial_india_061/`，任务 20260914_011909_1b7e95）在本轮写入时仍处于 S5：分方向组合与全局 empty/profit/risk 起点已局部停止，正在比较方向并集起点；S6/S7 未开始。该试跑绑定 0.6.1 引擎，不能混用 0.6.2 断点。（该证据目录已于 2026-09-16 按用户要求清理，本条只作历史记录。）
- 冻结与提交：最终回归通过后冻结到 `validation/frozen_releases/0.6.2_7ba63934e2cf`，在本地 Git 分支提交并打 `frozen-v0.6.2` 标签（不推送）。
- 2026-09-15 核对更正：上一条冻结与提交实际未完成。`validation/frozen_releases/` 中没有 0.6.2 目录，Git 中也没有 `frozen-v0.6.2` 标签；最新提交和标签仍是 0.6.1，0.6.2 起的修订均未提交（0.6.3 只保留冻结目录 `0.6.3_d3463b18c5ba`）。

以下为 0.6.1 及更早版本的历史证据，只认证其对应指纹：

# v0.6.1 正式使用修订

本轮目标是结束后即可正式使用：在 0.6.0 全历史研究的基础上，修复印度超真实试跑和三路审查发现的问题，不改变盈利池、历史筛选和取价口径。

组合比较（S5）：
- 印度超 0.6.0 试跑显示组合比较是主要瓶颈。全局搜索一步完整邻域约 6.4 万次组合比较，逐单完整回放约 81 毫秒一次；12 步改动上限和 20 万次评价上限还会让结果停在部分完成。
- 新增按场增量重放（`lab/portfolio_replay.py`）：组合变动时只重放涉及变动规则的比赛，其余比赛沿用结果。
  - 与逐单完整回放逐一对照无差异：随机对照测试（含相同分钟、相反方向、重复报价合约与整场上限），以及印度超真实候选池（1,670 条规则、24.6 万笔订单、325 场）的 600 个组合。
  - 每个组合约 2.4 毫秒；38 条起点的一步完整邻域约 31 秒。
- 历史模式默认组合改动与评价预算均为 0，即直到局部最优；断点每 4096 次比较或每 30 秒保存一次。

其他修订：
- 历史净胜：空白滚球比分按最后已知比分判断是否进球，不再把缺失比分当成比分改变。
- 最终决定：写出主名单与较稳定备选各自的共同回放合计（笔数、净胜、ROI、按场回撤），不把单条净胜相加。
- 界面：
  - 任一计算预算非零时，在开始按钮上方提示；0.6.0 保存的组合上限不再带入。
  - 演示任务不覆盖研究设置；历史任务隐藏样本外文件；可预览 `rules.json` 与接入清单。
  - 新增“全部续跑”；关闭界面服务前提示排队任务不会开始。
- 启动与调度：
  - 同一工作区已有其他版本的工作台时，提示先关闭它。
  - 代码更新后，旧引擎的排队任务改为暂停，不再启动后失败；新增 `rebuild-stale`（0.5 任务按原验证口径重建）与 `resume-jobs`。
  - 磁盘不足时新任务保持排队；`start_windows.bat` 使用 UTF-8 代码页。
- 数据检查：跳过“_”开头的文件夹（如 `_build` 备份）；同一联赛在多个目录有重复比赛时拒绝建任务。对真实“皇冠开盘至少200场”目录（1,359 个文件、979 个联赛）复查，无误报。
- 多机：
  - 任务按软件引擎、联赛、公司、输入哈希与研究配置识别；重新生成计划或重复导入不会重复建任务。
  - 节点计划列出该机需要复制的文件（`required_files`），导入时只核对本机联赛的报价与索引文件。
  - `--company` 与配置文件冲突时报错；之后再带 `--queue` 导入会把尚未开始的暂停任务排队。
- 新增测试：`test_formal_use_061`（8 项）与 `test_portfolio_replay_061`（3 项），并补充启动器端口测试。

本轮验证（最终引擎 4256fefd…，证据位于 `validation/revision_061/`）：
- 默认单元测试（最终源码副本）：432 项通过（2 项端到端默认跳过，另行运行），194 秒。
- 历史模式空结果链路：PASS，423 秒。3 场全部参与，盈利表达发生数 373，剩余 0，状态 HISTORICAL_RESEARCH_COMPLETE。
- 历史模式非空链路：PASS，565 秒。
  - 90 场全部参与，盈利表达发生数 47，主备名单各 4 条。
  - 方向内、全局与较稳定备选的组合比较全部使用按场增量重放，并达到局部最优（FINITE_LOCAL_SEARCH_COMPLETE）。
  - 主名单共同回放 360 笔，净胜 85.5 单位，按场回撤 8.0 单位。
  - 每方向 10 节点预算使剩余 1,315，正确标为部分结果。
- 验证模式空结果链路：PASS，343 秒。冻结表达 4,654 个：16 个实际评价，4,638 个由收益上界证明不过线，剩余 0；空名单链路认证通过。
- 验证模式非空链路：PASS，423 秒。结果与 0.5.0/0.6.0 相同：
  - 主名单 2 条：滚球让球方 60 笔、净胜 15.6；滚球大球 60 笔、净胜 22.8；回撤各 2.0。
  - 样本外确认；A 包解压后回放通过。
  - 验证模式仍按全部五个情景逐单回放比较组合，不使用增量重放。
- 浏览器界面（Chrome 无头模式，独立端口，工作区复制自历史非空验收任务并写入 0.6.0 保存的旧设置；未触碰 8765 服务）：28 项通过，无脚本错误。
- 依赖核对：PASS。
- 隔离冻结回归：写完本文件后对最终源码运行。

边界与后续：
- 本轮未修改复盘系统的现行策略和实盘配置，未运行真实下注。
- 印度超 0.6.0 历史试跑（冻结 0.6.0 副本）仍在组合比较阶段，按用户要求继续运行。0.6.1 冻结后，在独立试跑工作区用新版重跑印度超，作为真实联赛核对，结果另行记录，不写入本文件。
- 工作区里 0.5/0.6.0 创建的旧任务（暂停 4、中断 2）属于旧引擎，不会自动开始；需要时用 `python -B app.py rebuild-stale` 重建。
- 单个联赛任务目录可达数 GB（印度超 0.6.0 试跑到组合比较阶段约 2.5 GB），多联赛运行前请预留磁盘。
- 冻结与提交：最终回归通过后冻结到 `validation/frozen_releases/0.6.1_4256fefdef60`，在本地 Git 分支提交并打 `frozen-v0.6.1` 标签（不推送）。

以下为 0.6.0 及更早版本的历史证据，只认证其对应指纹：

# v0.6.0 全历史研究修订

核心指导思想已落实到文档、默认配置、盈利池、历史筛选、完成判断和界面。新增离线多机联赛计划与触发接入清单。新历史模式测试及旧验证模式回归分别保留。

定稿补充（2026-09-14）：历史净胜剔除进球前拒单。
- 滚球首次信号若在成交后 1 分钟内出现同场同市场封盘且比分改变，视为实际无法成交，按首次信号记零、不顺延补单。
- 盈利池、第二阶段资格、组合比较与经济门禁统一使用该口径；验证模式仍按 s4 单独核对，结果不变。
- 新增 4 项测试；原有两处测试期望按新口径更新。

本轮验证（最终引擎 f770bd8c…，证据位于 `validation/revision_060/`；以下各项同时运行）：
- 默认单元测试（最终源码副本）：421 项通过（2 项端到端默认跳过），180 秒。
- 历史模式空结果链路：PASS，402 秒。3 场全部参与，盈利表达发生数 373，剩余 0，状态 HISTORICAL_RESEARCH_COMPLETE。
- 历史模式非空链路：PASS，542 秒。90 场全部参与，盈利表达发生数 47，主备名单各 4 条，净胜按剔除进球前拒单的分钟末历史报价计算；每方向 10 节点预算使剩余 1,315，正确标为部分结果。
- 验证模式空结果链路 PASS（406 秒），非空链路 PASS（499 秒），结果与 0.5.0 一致：样本外确认 60 笔、净胜 +19.2，覆盖 20.40%。
- 浏览器界面（历史模式，Chrome 无头模式，独立端口，未触碰 8765 服务）：19 项通过，无脚本错误。
- 依赖核对：PASS。隔离冻结回归：写完本文件后对最终源码运行。

本轮不修改复盘系统的现行策略和实盘配置，不运行真实联赛研究。跨机安装/文件传输和复盘系统执行适配器尚未部署。

冻结与提交：最终回归通过后冻结到 `validation/frozen_releases/0.6.0_f770bd8ce987`，并在本地 Git 分支提交、打 `frozen-v0.6.0` 标签（不推送）。

以下为旧0.5版本的历史证据，只认证其对应指纹：

# v0.5.0 本版实测与未完成项

本版按合理性审查，把不合理的研究口径改为与样本量相关的合理口径，并新增默认样本外留出。发布验收未启动真实联赛研究；旧断点、旧源码与历史证据保留；真实资金和第二笔关闭。

实测环境：Windows x64 / Python 3.12.8，项目 .venv：numpy 2.3.5、numba 0.65.1、llvmlite 0.47.0。最终引擎指纹 5cd51933…（FSL_ENGINE_INPUTS_V5），SBOM 已同步。定稿包含其他会话的兼容改动，均随下列验收在最终源码上一并通过：
- 2026-09-13 21:49—21:51：持久化模拟账也按拒单规则核对进球前拒单情景；worker 退出码与调度小修；扫描缓存版本升级与说明文字。
- 2026-09-14 00:05—00:12：
  - 默认端口被其他程序占用时，自动改用空闲本机端口；下次启动先校验源码、版本与工作区身份，再复用该实例。
  - Windows 监听使用独占绑定，防止两个服务共用一个端口。
  - 旧版无附加条件规则的空证据对象可以正常回放，缺失或篡改的证据仍拒绝。
  - 新增 9 项针对性测试。
- 2026-09-14 00:46—01:01：
  - 结果预览加固：大型 JSON/Markdown 只预览前 1 MiB 并提示截断，下载保留全部内容；切换结果或翻页会取消上一条未完成的预览；预览索引与 JSON 缓存加锁；CSV 引号未闭合时报错，不再卡住。
  - 拒单情景 s4 纳入交接核对：标准冻结包必须声明进球前拒单政策；s4 写出 `orders_s4` 与 `minute_close_orders_s4`，作为档案计价情景（不经流式协调器）进入核对报告；经济门禁按五个已计价情景核对，交接 s4 组合须与独立重算一致。
  - 调度：计算进程以 0 退出但任务仍在排队（尚未领取）时保持排队，不标为错误。
  - 新增 11 项测试（预览可靠性 7、交付修复 3、外部审查 1）。
- 界面验收时补充：结果预览可直接打开 最终决定.md、缺口说明.md、样本外评测.json 与 样本外逐条.csv。
- 上述改动使引擎指纹由 c358c6a3… 依次变为 3cec480c…、5cd51933…。最后一轮改动未更新 SBOM，治理测试因此失败；用 `validation/update_sbom.py` 重新生成后，下列验收全部在 5cd51933… 上重跑。

注意：若 8765 上已有版本或源码不同的工作台（例如未重启的旧服务），双击启动不再拒绝，而是在空闲端口为同一工作区再开一个服务。任务领取有锁保护，但建议先关闭旧服务再启动。

端到端验收与冻结回归由 `validation/frozen_regression.py` 在冻结副本上运行，结束时核对源码与副本均未改变。验收证据位于 `validation/revision_050/`。

- **默认单元测试**（最终源码副本）：406 项通过（2 项端到端流水线默认跳过，另行运行），142 秒（与两条端到端链路同时运行）。其中 `tests/test_reasonable_050.py`（20 项）覆盖：
  - 配置：废弃绝对门槛字段被拒绝、标准档锁定；
  - 单条门槛：按方向可用场次折算场次，研究段覆盖与“去掉最好研究段”，收益回撤比（大样本真实优势通过、小样本深回撤淘汰）；
  - 组合：收益回撤比不再被绝对回撤封顶，利润容差为相对值；
  - 样本外：拆分、短数据、评测结论、检验次数披露，以及推荐结论；
  - 赛前：只在即时盘触发，跨市场陈旧度24小时，方向可用场次统计；
  - 搜索：紧凑语法、恒零特征不入搜索、邻域步长；
  - 工程：并行槽位与领取上限，非计算模块不改变引擎指纹。

  其他会话补充的 5 项、9 项与 11 项测试同样通过。
- **16 分支空结果链路**（合成 36 条报价，默认紧凑语法）：PASS，309 秒（与非空链路同时运行；0.4.9 为 1,281 秒）。
  - 最近 12 个月的 1 场在读取后即留出，发现只用 2 场。
  - 冻结表达 4,654 个：16 个代表实际评价，4,638 个由收益上界证明不过线，剩余 0，搜索完整。
  - 名单为空，样本外结论为“名单为空”；只缺“非空主名单”门禁，空链路认证通过；结论“仅观察”，原因只有名单为空。
- **16 分支非空有限链路**（合成 90 场、每方向 10 节点预算）：PASS，383 秒（与空链路同时运行；0.4.9 为 1,818 秒）。
  - 最近 12 个月（2024 年的 30 场）在读取后即留出；发现与选择只用 2022—2023 年的 60 场，两个研究段各 30 场。
  - 全池复核 27 个盈利表达发生数、25 条规范规则、44 条邻域与报价丢失记录，得到 3 个合格信号组；组合比较出现真实的加入、移除与替换。
  - 主名单与较低风险名单各 2 条（合成数据下两者相同）：
    - 滚球让球方：无附加条件，60 笔，净胜 15.6，回撤 2.0，收益回撤比 7.8；
    - 滚球大球：买入水位≥1.15，60 笔，净胜 22.8，回撤 2.0，收益回撤比 11.4。
  - 组合：120 笔，净胜 38.4，比赛级回撤 4.0。
  - 样本外（2024 年 30 场）：主备均“确认”。60 笔，净胜 19.2 单位，ROI 32%，z 2.27，五个情景中最低净胜 17.4。
  - 检验次数：实际评价 33 个不同信号类，按次数校正后仍显著的为 0/2 条（报告如实写出）。
  - 冻结语法覆盖 20.40%（受 10 节点预算限制，符合预期）；结论“仅观察”，原因写明搜索未完成。
  - 五个情景的订单与独立重算差异均为 0；持久化模拟账 s0—s4 各 120 笔一致；黄金信号 120 条。
  - 流式核对 s0—s3 通过；拒单情景 s4 档案计价 120 笔，与独立重算差异 0（不经流式协调器）；经济门禁按 5 个已计价情景核对通过。
  - 真实 A 包在空目录中解压后，引擎自检与逐条回放均通过。
- **隔离冻结回归**（引擎自检）：写完本文件后对最终源码运行。
- **浏览器界面验收**（Chrome 153 无头模式，经 DevTools 协议操作；独立端口，工作区复制自最终源码的非空验收任务；未触碰 8765 服务）：24 项全部通过，页面无脚本错误。
  - 标准档的 11 个研究门槛输入全部锁定并显示标准值，页面上没有漏锁的标准门槛；切到旧版档位可改，切回后重新锁定；废弃门槛输入已移除。
  - 并行任务数显示工作区设置，改为 2 后自动保存到调度设置。接口冒烟另测：非法值返回 400，标准档里被放宽的比例会锁回 2，废弃字段不会被保存。
  - 任务卡片显示“结论：仅观察，不推荐进入模拟”及原因（搜索未完成，覆盖 20.3995%）、样本外“确认为正 · 订单 60”、未通过项“完整标准搜索”。
  - 结果预览能打开最终决定、缺口说明、样本外评测与样本外逐条，并可下载；主名单表含收益回撤比、研究段场次与 z 值列。
  - 这是功能验收，不是可用性评估。
- **依赖核对**（`validation/check_dependencies.py`）：PASS。安装版本与锁文件一致，锁定哈希均在 PyPI 官方发布中，未查到已知漏洞。
- **真实数据只读核对**（印度超三年，未建研究任务）：
  - 即时盘在开赛前 0—10.8 小时，650 个比赛×市场中 649 个有即时盘报价；早盘在开赛前 7.5—168 小时。
  - 赛前跨市场报价年龄 > 5 分钟占 67.5%、> 24 小时占 3.7%；滚球 > 5 分钟占 1.6%。
  - 以最后比赛日为锚的 12 个月窗口与赛季吻合（71 / 163 / 91 场）。
  - 紧凑语法在 16 个方向共 2,568 个单条件（每方向 99—205 个）、约 21 万个两两组合节点；v3 完整语法仅赛前让球方就约 2.9 万亿个表达。
- **门槛模拟**（按实测结算分布，每组 4 万次）：

  | 场次 | 规则 | 新门槛通过率 | 旧门槛通过率 |
  |---:|---|---:|---:|
  | 300 | 真实 ROI +5% | 20.3% | 0.16% |
  | 300 | ROI −3%（运气） | 0.83% | 0.01% |
  | 80 | 真实 ROI +5% | 12.4% | 8.9% |
  | 80 | ROI −3%（运气） | 2.6% | 1.9% |

  300 场时优势与运气的通过率之比为 24.5，旧门槛为 22。

仍未认证：
- 真实联赛非空的完整 16 分支标准搜索与交接；
- 数百联赛的批量容量；
- 真实滚动与真正的未来数据；
- 上游赛果的真实性；
- 实际行情、成交与统一账户层。

边界说明：v3 完整语法在真实联赛上算不完；真实资金和第二笔未开放；默认样本外留出是历史检验，不是未来实盘证据。

待用户决定：复合 T 是否登记进 v3 完整语法。

冻结与提交：本文件定稿后运行最终隔离回归，通过后冻结到 `validation/frozen_releases/0.5.0_5cd519330881`，并在本地 Git 分支提交、打 `frozen-v0.5.0` 标签（不推送）。印度超完整标准试跑在冻结副本上另行运行，结果记录在 `validation/trial_india_050/`，不写入本文件（该证据目录已于 2026-09-16 清理）。8765 界面服务如仍运行旧代码，需重启后才显示新设置。
